{
  "name": "audio-network-lite",
  "version": "0.1.0-rc15",
  "description": "",
  "keywords": [],
  "author": "Robert Rypuła",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn lint && yarn prettier && yarn test:coverage && webpack --env.PRODUCTION",
    "clean": "rimraf dist && rimraf coverage",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "npm-check": "npm-check --skip-unused -u",
    "prettier": "prettier src/**/*.{ts,html,scss} -c",
    "start": "yarn clean && webpack-dev-server --env.DEVELOPMENT --open",
    "start:node": "yarn clean && webpack --env.DEVELOPMENT && && node dist/audio-network-lite-v0.1.0-rc15.js run.cliNodeExample",
    "test": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:report-coverage": "cat ./coverage/lcov.info | coveralls"
  },
  "main": "dist/audio-network-lite-v0.1.0-rc15.js",
  "types": "dist/lib/index.d.ts",
  "files": [
    "dist/"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/robertrypula/audio-network-lite.git"
  },
  "bugs": {
    "url": "https://github.com/robertrypula/audio-network-lite/issues"
  },
  "devDependencies": {
    "@types/jest": "^24.0.17",
    "@types/node": "^12.7.2",
    "coveralls": "^3.0.6",
    "css-loader": "^3.2.0",
    "html-webpack-plugin": "^3.2.0",
    "jest": "^24.9.0",
    "node-sass": "^4.12.0",
    "npm-check": "^5.9.0",
    "prettier": "1.18.2",
    "rimraf": "^3.0.0",
    "sass-loader": "^8.0.0",
    "ts-jest": "^24.0.2",
    "ts-loader": "^6.0.4",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3",
    "typescript-formatter": "^7.2.2",
    "uglifyjs-webpack-plugin": "^2.2.0",
    "webpack": "^4.39.2",
    "webpack-cli": "^3.3.6",
    "webpack-dev-server": "^3.8.0"
  }
}
