{
  "name": "audio-network-reborn",
  "version": "0.1.0-rc18",
  "description": "",
  "keywords": [],
  "author": "Robert Rypuła",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn lint && yarn prettier && yarn test:coverage && webpack --env.PRODUCTION",
    "build:analyzer": "yarn clean && webpack --env.PRODUCTION --env.ANALYZER",
    "clean": "rimraf dist && rimraf coverage",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "prettier": "prettier src/**/*.{ts,js,html,scss} -c",
    "prettier:write": "prettier --write src/**/*.{ts,js,html,scss}",
    "start": "yarn clean && webpack-dev-server --env.DEVELOPMENT --open",
    "start:node": "yarn clean && webpack --env.DEVELOPMENT && yarn start:node:existing",
    "start:node:existing": "node dist/audio-network-reborn-v0.1.0-rc18.js audioNetworkReborn.run.nodeExample",
    "test": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:report-coverage": "cat ./coverage/lcov.info | coveralls",
    "u:i": "yarn upgrade-interactive --latest"
  },
  "main": "dist/audio-network-reborn-v0.1.0-rc18.js",
  "types": "dist/lib/index.d.ts",
  "files": [
    "dist/"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/robertrypula/audio-network-reborn.git"
  },
  "bugs": {
    "url": "https://github.com/robertrypula/audio-network-reborn/issues"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.8",
    "coveralls": "^3.0.6",
    "css-loader": "^3.2.0",
    "html-loader": "^0.5.5",
    "html-webpack-plugin": "^3.2.0",
    "jest": "^24.9.0",
    "node-sass": "^4.12.0",
    "prettier": "1.18.2",
    "rimraf": "^3.0.0",
    "sass-loader": "^8.0.0",
    "ts-jest": "^24.1.0",
    "ts-loader": "6.2.0",
    "tslint": "^5.20.0",
    "typescript": "^3.6.3",
    "webpack": "^4.41.0",
    "webpack-bundle-analyzer": "^3.5.2",
    "webpack-cli": "^3.3.9",
    "webpack-dev-server": "^3.8.1"
  }
}
